<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.ayroot.calculator.MainActivity"
    android:orientation="vertical">
    <TextView
        android:id="@+id/info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="textEnd"
        android:gravity="end"
        android:textSize="60sp"
        android:text="Поле" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_below="@+id/info">
        <GridView
            android:id="@+id/gridView"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:numColumns="3">
        </GridView>
        <GridLayout
            android:id="@+id/operation"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="0.85"
            android:columnCount="2">
            <Button android:text="@string/result"
                android:tag="="
                android:id="@+id/res"
                />
            <Button android:text="@string/clean"
                android:tag="C"/>
            <Button android:text="@string/sum"
                android:tag="+"/>
            <Button android:text="@string/sub"
                android:tag="-"/>
            <Button android:text="@string/mlp"
                android:tag="*"/>
            <Button android:text="@string/div"
                android:tag="/"/>
        </GridLayout>
    </LinearLayout>
</RelativeLayout>


